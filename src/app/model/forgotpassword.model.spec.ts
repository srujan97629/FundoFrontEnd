import { ForgotpasswordModel } from './forgotpassword.model';

describe('Forgotpassword.Model', () => {
  it('should create an instance', () => {
    expect(new ForgotpasswordModel()).toBeTruthy();
  });
});
