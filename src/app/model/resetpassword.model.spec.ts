import { Resetpassword.Model } from './resetpassword.model';

describe('Resetpassword.Model', () => {
  it('should create an instance', () => {
    expect(new Resetpassword.Model()).toBeTruthy();
  });
});
